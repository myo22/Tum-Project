package myo.pro.first.EX;

public class Rectangle2 {
    public static void main(String[] args){
        int i = 1;
        while (i <= 10){
            System.out.println("**********");
            i = i + 1;
        }

    }
}

/* int 정수 type  int i 변수 = 값을 가질 수 있다.
정수 = 음수 0 양수  i = 4byte 메모리 공간에 값이 저장된다.

int 정수 4byte
short 정수 2byte
long 정수 8byte
float                 기본형 type(첫번째 글자가 소문자다) 다 크기를 가지고 있습니다. ex) 4byte
double
char
boolean

기본형 변수 값은 가진다.
Book b = new Book(); 이것처럼 기본형이 아닌 type은 모두 대문자이다.(class interface 등등.....)
기본형이 아닌 타입은 값을 가지지 않고 참조한다 = reference 타입(참조형 타입)

boolean을 초기화 하지 않으면 기본적으로 false값을 가진다.
클래스 안에 선언된 변수는 필드(feild)라고 합니다 이건 값을 초기화 하지 않아도 괜찮다. 메소드 안에 선언된 변수는 초기화 하지 않으면 오류가 난다.
 */

